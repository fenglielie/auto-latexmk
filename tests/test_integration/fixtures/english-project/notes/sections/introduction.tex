\section{Introduction}

This section is stored in a nested input file. It must not be discovered as an
independent document because it has no document class.

The project exercises relative paths, references, and the reruns managed by
\texttt{latexmk}.
